const Post = require('../Post')
const User = require('../User')
const Mutation = require('../Mutation')
const Query = require('../Query')

const resolvers = {
  Mutation,
  Query,
  Post,
  User
}

test('creating draft', async () => {})

test('creating publish fails', async () => {})
